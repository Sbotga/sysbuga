# Discord
discord.py

# Database
asyncpg

# Config
pyyaml

# Network
aiohttp
brotli>=1.2  # aiohttp 3.13+ passes max_length to the decompressor; older Brotli breaks on Discord's br responses

# Public API server
fastapi
uvicorn

# Data models
pydantic

# Local fuzzy search (song/event matching, JP/KR/ZH romanization)
rapidfuzz
cutlet

# mojimoji (cutlet dep) has no PyPI wheels past cp312 and its old sdist doesn't
# compile on 3.13+. Master builds fine (needs a C++ compiler + Cython, handled
# by its pyproject). Drop this once upstream ships new wheels
# (https://github.com/studio-ousia/mojimoji/pull/28).
mojimoji @ git+https://github.com/studio-ousia/mojimoji@2fb1f46c96f716da596f02707fdfae8621be6c98 ; python_version >= "3.13"

unidic
korean-romanizer==0.28.0
pinyin-jyutping==0.9

# Images
pillow
numpy

# Console
colorama

# redis
redis

# compress saved per-event snapshots/profiles once an event is past
zstandard

# websockets
websockets
# chart clip rendering (sus cut + LevelData export for /guess chart)
sonolus_converters @ git+https://github.com/UntitledCharts/sonolus-level-converters

# timezone database for zoneinfo (heatmap hour/day bucketing); Windows lacks a system one
tzdata

# fast json parsing of the event snapshot history (streamed for /event heatmap)
orjson
